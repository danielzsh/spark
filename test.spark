main() {
 	vars:
		int: a, b, d;
		real: c;
		string: test, multi;
		bool: yes;
	def foo (int: a; bool: b;) -> int {
		def sub (int: a) {
			program:
				print('{a}');		
		}
		program:
			print('Delegating print to sub-function: ');
			sub(a);
			print('Sub completed');	
	} -> return 1
	def retStatments(int: a) -> int {
		def subtract(int: a) -> int {
		} -> return a - 1
	} -> return subtract(a)
	program: #this is a comment#
		a = 1*1+4//-2;
		b = 12-13;
		b = retStatments(b);
		yes = a == b;
		a = foo(a, yes);
		if: yes -> {
			program:
				print('if statements work now');
		}
		}
		test = 'The value of a is {a}, and the value of b is {b} ' + 'some concatenated message ' * 2;
		multi = 'a*b={a*b}';
		print('{test}');
		print('{multi}');
 		d = 40;
 		c = (a - d)//3;
		{
			program:
				c = (a - d)//3;
		}
	}
