 main() {
 	vars:
		int: a, c, d;
		real: b;
		string: test, multi;
	def foo (int: bar) {
		def proc (int: asdf) {
			program: a = asdf;
		}
		program:	
			bar = a;
			proc(5);
	}
	program: #this is a comment#
		foo(a);
		a = 1*1+4//-2;
		b = -13+4.5;
		test = 'The value of a is {a}, and the value of b is {b}';
		multi = 'a*b={a*b}';
		print('{test}');
		print('{multi}');
 		c = (a - d)//3;
 		d = 40;
		{
			program:
				c = (a - d)//3;
		}
	}
